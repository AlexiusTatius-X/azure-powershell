## 16.0.0 - June 2026
#### Az.CloudService 3.0.0 
* Modified cmdlet `Get-AzCloudServiceNetworkInterface`
   - Added parameters `-CloudServiceInputObject`, `-RoleInstanceInputObject`
* Modified cmdlet `Get-AzCloudServiceOSFamily`
   - Added parameter `-LocationInputObject`
* Modified cmdlet `Get-AzCloudServiceOSVersion`
   - Added parameter `-LocationInputObject`
* Modified cmdlet `Get-AzCloudServicePublicIPAddress`
   - Added parameters `-CloudServiceInputObject`, `-IpconfigurationInputObject`, `-NetworkInterfaceInputObject`, `-RoleInstanceInputObject`
* Modified cmdlet `Get-AzCloudServiceRoleInstance`
   - Added parameter `-RoleInstanceInputObject`
   - Changed the type of parameter `-Expand` from `InstanceViewTypes` to `String`
* Modified cmdlet `Get-AzCloudServiceRoleInstanceRemoteDesktopFile`
   - Added parameter `-RoleInstanceInputObject`
* Modified cmdlet `Get-AzCloudServiceRoleInstanceView`
   - Added parameter `-RoleInstanceInputObject`
* Modified cmdlet `Invoke-AzCloudServiceRebuild`
   - Added parameters `-JsonString`, `-JsonFilePath`
* Modified cmdlet `New-AzCloudService`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-UpgradeMode` from `CloudServiceUpgradeMode` to `String`
* Modified cmdlet `Remove-AzCloudServiceRoleInstance`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Restart-AzCloudService`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Restart-AzCloudServiceRoleInstance`
   - Added parameter `-RoleInstanceInputObject`
* Modified cmdlet `Set-AzCloudServiceUpdateDomain`
   - Added parameters `-JsonFilePath`, `-JsonString`
#### Az.Compute 11.6.0 
* Modified cmdlet `New-AzRestorePoint`
   - Added parameter `-InstantAccessDurationInMinutes`
* Modified cmdlet `New-AzRestorePointCollection`
   - Added parameter `-InstantAccess`
* Modified cmdlet `Update-AzRestorePointCollection`
   - Added parameter `-InstantAccess`
#### Az.ContainerInstance 5.0.0 
* Modified cmdlet `Add-AzContainerInstanceOutput`
   - Added parameter `-ContainerGroupInputObject`
* Modified cmdlet `Get-AzContainerInstanceContainerGroupProfile`
   - Added parameter `-ContainerGroupProfileInputObject`
* Modified cmdlet `New-AzContainerGroup`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-ConfidentialComputePropertyCcePolicy`, `-EnableSystemAssignedIdentity`, `-EncryptionPropertyIdentity`, `-Extension`, `-IPAddressAutoGeneratedDomainNameLabelScope`, `-UserAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-OSType` from `OperatingSystemTypes` to `String`
   - Changed the type of parameter `-IPAddressType` from `ContainerGroupIPAddressType` to `String`
   - Changed the type of parameter `-LogAnalyticLogType` from `LogAnalyticsLogType` to `String`
   - Changed the type of parameter `-RestartPolicy` from `ContainerGroupRestartPolicy` to `String`
   - Changed the type of parameter `-Sku` from `ContainerGroupSku` to `String`
* Modified cmdlet `New-AzContainerInstanceContainerGroupProfile`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-OSType` from `OperatingSystemTypes` to `String`
   - Changed the type of parameter `-IPAddressAutoGeneratedDomainNameLabelScope` from `DnsNameLabelReusePolicy` to `String`
   - Changed the type of parameter `-IPAddressType` from `ContainerGroupIPAddressType` to `String`
   - Changed the type of parameter `-LogAnalyticLogType` from `LogAnalyticsLogType` to `String`
   - Changed the type of parameter `-Priority` from `ContainerGroupPriority` to `String`
   - Changed the type of parameter `-RestartPolicy` from `ContainerGroupRestartPolicy` to `String`
   - Changed the type of parameter `-Sku` from `ContainerGroupSku` to `String`
* Modified cmdlet `New-AzContainerInstanceObject`
   - Added parameter `-SecurityContextSeccompProfile`
* Modified cmdlet `Remove-AzContainerInstanceSubnetServiceAssociationLink`
   - Added parameter `-VirtualNetworkInputObject`
* Modified cmdlet `Update-AzContainerGroup`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzContainerInstanceContainerGroupProfile`
   - Added parameters `-JsonFilePath`, `-JsonString`
#### Az.Databricks 2.0.0 
* Modified cmdlet `Get-AzDatabricksVNetPeering`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `New-AzDatabricksAccessConnector`
   - Removed parameter `-IdentityType`
   - Added parameters `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-UserAssignedIdentity` from `Hashtable` to `String[]`
* Modified cmdlet `New-AzDatabricksVNetPeering`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDatabricksWorkspace`
   - Added parameters `-DefaultCatalogInitialName`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-DefaultCatalogInitialType` from `InitialType` to `String`
   - Changed the type of parameter `-EncryptionKeySource` from `KeySource` to `String`
   - Changed the type of parameter `-ManagedDiskKeySource` from `EncryptionKeySource` to `String`
   - Changed the type of parameter `-ManagedServiceKeySource` from `EncryptionKeySource` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccess` to `String`
   - Changed the type of parameter `-RequiredNsgRule` from `RequiredNsgRules` to `String`
   - Changed the type of parameter `-EnhancedSecurityMonitoring` from `EnhancedSecurityMonitoringValue` to `String`
   - Changed the type of parameter `-AutomaticClusterUpdate` from `AutomaticClusterUpdateValue` to `String`
   - Changed the type of parameter `-EnhancedSecurityCompliance` from `ComplianceSecurityProfileValue` to `String`
   - Changed the type of parameter `-AccessConnectorIdentityType` from `IdentityType` to `String`
   - Changed the type of parameter `-DefaultStorageFirewall` from `DefaultStorageFirewall` to `String`
* Modified cmdlet `Remove-AzDatabricksVNetPeering`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Update-AzDatabricksAccessConnector`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
* Modified cmdlet `Update-AzDatabricksVNetPeering`
   - Added parameters `-JsonString`, `-JsonFilePath`
   - Changed the type of parameter `-AllowForwardedTraffic` from `Boolean` to `SwitchParameter`
   - Changed the type of parameter `-AllowGatewayTransit` from `Boolean` to `SwitchParameter`
   - Changed the type of parameter `-AllowVirtualNetworkAccess` from `Boolean` to `SwitchParameter`
   - Changed the type of parameter `-UseRemoteGateway` from `Boolean` to `SwitchParameter`
* Modified cmdlet `Update-AzDatabricksWorkspace`
   - Changed the type of parameter `-EncryptionKeySource` from `KeySource` to `String`
   - Changed the type of parameter `-DefaultCatalogInitialType` from `InitialType` to `String`
   - Changed the type of parameter `-ManagedDiskKeySource` from `EncryptionKeySource` to `String`
   - Changed the type of parameter `-ManagedServiceKeySource` from `EncryptionKeySource` to `String`
   - Changed the type of parameter `-RequiredNsgRule` from `RequiredNsgRules` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccess` to `String`
   - Changed the type of parameter `-EnhancedSecurityMonitoring` from `EnhancedSecurityMonitoringValue` to `String`
   - Changed the type of parameter `-AutomaticClusterUpdate` from `AutomaticClusterUpdateValue` to `String`
   - Changed the type of parameter `-EnhancedSecurityCompliance` from `ComplianceSecurityProfileValue` to `String`
   - Changed the type of parameter `-AccessConnectorIdentityType` from `IdentityType` to `String`
   - Changed the type of parameter `-DefaultStorageFirewall` from `DefaultStorageFirewall` to `String`
#### Az.DataProtection 3.0.0 
* Modified cmdlet `Backup-AzDataProtectionBackupInstanceAdhoc`
   - Added parameters `-BackupVaultInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Find-AzDataProtectionRestorableTimeRange`
   - Added parameters `-BackupVaultInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-SourceDataStoreType` from `RestoreSourceDataStoreType` to `String`
* Modified cmdlet `Get-AzDataProtectionBackupInstance`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Get-AzDataProtectionBackupPolicy`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Get-AzDataProtectionOperationStatus`
   - Added parameter `-LocationInputObject`
* Modified cmdlet `Get-AzDataProtectionRecoveryPoint`
   - Added parameters `-BackupInstanceInputObject`, `-BackupVaultInputObject`
* Modified cmdlet `Get-AzDataProtectionResourceGuardMapping`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Get-AzDataProtectionSoftDeletedBackupInstance`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Get-AzDataProtectionSoftDeletedBackupVault`
   - Added parameter `-LocationInputObject`
* Modified cmdlet `Initialize-AzDataProtectionRestoreRequest`
   - Added parameter `-RenameTo`
* Modified cmdlet `New-AzDataProtectionBackupVault`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
   - Changed the type of parameter `-AzureMonitorAlertsForAllJobFailure` from `AlertsState` to `String`
   - Changed the type of parameter `-ImmutabilityState` from `ImmutabilityState` to `String`
   - Changed the type of parameter `-CrossRegionRestoreState` from `CrossRegionRestoreState` to `String`
   - Changed the type of parameter `-CrossSubscriptionRestoreState` from `CrossSubscriptionRestoreState` to `String`
   - Changed the type of parameter `-SoftDeleteState` from `SoftDeleteState` to `String`
   - Changed the type of parameter `-CmkEncryptionState` from `EncryptionState` to `String`
   - Changed the type of parameter `-CmkInfrastructureEncryption` from `InfrastructureEncryptionState` to `String`
   - Changed the type of parameter `-CmkIdentityType` from `IdentityType` to `String`
* Modified cmdlet `Remove-AzDataProtectionBackupInstance`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Remove-AzDataProtectionBackupPolicy`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Remove-AzDataProtectionResourceGuardMapping`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Resume-AzDataProtectionBackupInstanceProtection`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Set-AzDataProtectionResourceGuardMapping`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Start-AzDataProtectionBackupInstanceRestore`
   - Changed the type of parameter `-SourceDataStoreType` from `SourceDataStoreType` to `String`
* Modified cmdlet `Stop-AzDataProtectionBackupInstanceProtection`
   - Added parameters `-BackupVaultInputObject`, `-Parameter`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Suspend-AzDataProtectionBackupInstanceBackup`
   - Added parameters `-BackupVaultInputObject`, `-Parameter`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Sync-AzDataProtectionBackupInstance`
   - Added parameters `-BackupVaultInputObject`, `-Parameter`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-SyncType` from `SyncType` to `String`
* Modified cmdlet `Undo-AzDataProtectionBackupInstanceDeletion`
   - Added parameter `-BackupVaultInputObject`
* Modified cmdlet `Undo-AzDataProtectionVaultDeletion`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
* Modified cmdlet `Update-AzDataProtectionBackupInstance`
   - Removed parameter `-BackupInstanceName`
   - Added parameter `-Name`
* Modified cmdlet `Update-AzDataProtectionBackupVault`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-XmsDeletedVaultId`, `-ETag`, `-EnableSystemAssignedIdentity`, `-ReplicatedRegion`, `-StorageSetting`, `-UserAssignedIdentity`
   - Changed the type of parameter `-AzureMonitorAlertsForAllJobFailure` from `AlertsState` to `String`
   - Changed the type of parameter `-CrossRegionRestoreState` from `CrossRegionRestoreState` to `String`
   - Changed the type of parameter `-CrossSubscriptionRestoreState` from `CrossSubscriptionRestoreState` to `String`
   - Changed the type of parameter `-ImmutabilityState` from `ImmutabilityState` to `String`
   - Changed the type of parameter `-SoftDeleteState` from `SoftDeleteState` to `String`
   - Changed the type of parameter `-CmkEncryptionState` from `EncryptionState` to `String`
   - Changed the type of parameter `-CmkIdentityType` from `IdentityType` to `String`
#### Az.DnsResolver 2.0.0 
* Modified cmdlet `Get-AzDnsForwardingRulesetForwardingRule`
   - Added parameter `-DnsForwardingRulesetInputObject`
* Modified cmdlet `Get-AzDnsForwardingRulesetVirtualNetworkLink`
   - Added parameter `-DnsForwardingRulesetInputObject`
* Modified cmdlet `Get-AzDnsResolverInboundEndpoint`
   - Added parameter `-DnsResolverInputObject`
* Modified cmdlet `Get-AzDnsResolverOutboundEndpoint`
   - Added parameter `-DnsResolverInputObject`
* Modified cmdlet `Get-AzDnsResolverPolicyDnsSecurityRule`
   - Added parameter `-DnsResolverPolicyInputObject`
* Modified cmdlet `Get-AzDnsResolverPolicyVirtualNetworkLink`
   - Added parameter `-DnsResolverPolicyInputObject`
* Modified cmdlet `New-AzDnsForwardingRuleset`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsForwardingRulesetForwardingRule`
   - Added parameters `-DnsForwardingRulesetInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-ForwardingRuleState` from `ForwardingRuleState` to `String`
* Modified cmdlet `New-AzDnsForwardingRulesetVirtualNetworkLink`
   - Added parameters `-DnsForwardingRulesetInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsResolver`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsResolverDomainList`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsResolverInboundEndpoint`
   - Added parameters `-DnsResolverInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsResolverIPConfigurationObject`
   - Changed the type of parameter `-PrivateIPAllocationMethod` from `IPAllocationMethod` to `String`
* Modified cmdlet `New-AzDnsResolverOutboundEndpoint`
   - Added parameters `-DnsResolverInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsResolverPolicy`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzDnsResolverPolicyDnsSecurityRule`
   - Removed parameter `-ActionBlockResponseCode`
   - Added parameters `-DnsResolverPolicyInputObject`, `-ManagedDomainList`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-ActionType` from `ActionType` to `String`
   - Changed the type of parameter `-DnsSecurityRuleState` from `DnsSecurityRuleState` to `String`
* Modified cmdlet `New-AzDnsResolverPolicyVirtualNetworkLink`
   - Added parameters `-DnsResolverPolicyInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Remove-AzDnsForwardingRulesetForwardingRule`
   - Added parameter `-DnsForwardingRulesetInputObject`
* Modified cmdlet `Remove-AzDnsForwardingRulesetVirtualNetworkLink`
   - Added parameter `-DnsForwardingRulesetInputObject`
* Modified cmdlet `Remove-AzDnsResolverInboundEndpoint`
   - Added parameter `-DnsResolverInputObject`
* Modified cmdlet `Remove-AzDnsResolverOutboundEndpoint`
   - Added parameter `-DnsResolverInputObject`
* Modified cmdlet `Remove-AzDnsResolverPolicyDnsSecurityRule`
   - Added parameter `-DnsResolverPolicyInputObject`
* Modified cmdlet `Remove-AzDnsResolverPolicyVirtualNetworkLink`
   - Added parameter `-DnsResolverPolicyInputObject`
* Modified cmdlet `Update-AzDnsForwardingRuleset`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsForwardingRulesetForwardingRule`
   - Added parameters `-DnsForwardingRulesetInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-ForwardingRuleState` from `ForwardingRuleState` to `String`
* Modified cmdlet `Update-AzDnsForwardingRulesetVirtualNetworkLink`
   - Added parameters `-DnsForwardingRulesetInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsResolver`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsResolverDomainList`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsResolverInboundEndpoint`
   - Added parameters `-DnsResolverInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsResolverOutboundEndpoint`
   - Added parameters `-DnsResolverInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsResolverPolicy`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzDnsResolverPolicyDnsSecurityRule`
   - Removed parameter `-ActionBlockResponseCode`
   - Added parameters `-DnsResolverPolicyInputObject`, `-ManagedDomainList`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-ActionType` from `ActionType` to `String`
   - Changed the type of parameter `-DnsSecurityRuleState` from `DnsSecurityRuleState` to `String`
* Modified cmdlet `Update-AzDnsResolverPolicyVirtualNetworkLink`
   - Added parameters `-DnsResolverPolicyInputObject`, `-JsonFilePath`, `-JsonString`
* Added cmdlet `Invoke-AzDnsResolverBulkDnsResolverDomainList`
#### Az.Functions 5.0.0 
* Modified cmdlet `New-AzFunctionApp`
   - Removed parameters `-IdentityType`, `-IdentityID`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
* Modified cmdlet `Update-AzFunctionApp`
   - Removed parameters `-IdentityType`, `-IdentityID`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
#### Az.LoadTesting 2.0.0 
* Modified cmdlet `New-AzLoad`
   - Removed parameters `-IdentityType`, `-IdentityUserAssigned`
   - Added parameters `-JsonFilePath`, `-JsonString`, `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
* Modified cmdlet `Update-AzLoad`
   - Removed parameters `-IdentityType`, `-IdentityUserAssigned`
   - Added parameters `-JsonFilePath`, `-JsonString`, `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
#### Az.MachineLearningServices 2.0.0 
* Modified cmdlet `Get-AzMLWorkspaceBatchDeployment`
   - Added parameters `-BatchEndpointInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Get-AzMLWorkspaceBatchEndpoint`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Get-AzMLWorkspaceCodeVersion`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Get-AzMLWorkspaceComponentContainer`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceComponentVersion`
   - Added parameters `-ComponentInputObject`, `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceCompute`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Get-AzMLWorkspaceConnection`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Get-AzMLWorkspaceDataContainer`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceDatastore`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-SearchName` from `String[]` to `List`1[System.String]`
* Modified cmdlet `Get-AzMLWorkspaceDataVersion`
   - Added parameters `-DataInputObject`, `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceEnvironmentContainer`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceEnvironmentVersion`
   - Added parameters `-EnvironmentInputObject`, `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceJob`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceModelContainer`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceModelVersion`
   - Added parameters `-ModelInputObject`, `-WorkspaceInputObject`
   - Changed the type of parameter `-ListViewType` from `ListViewType` to `String`
* Modified cmdlet `Get-AzMLWorkspaceOnlineDeployment`
   - Added parameters `-OnlineEndpointInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Get-AzMLWorkspaceOnlineDeploymentLog`
   - Added parameters `-InputObject`, `-OnlineEndpointInputObject`, `-WorkspaceInputObject`
   - Changed the type of parameter `-ContainerType` from `ContainerType` to `String`
   - Output type changed from ``String`` to ``IDeploymentLogs``
* Modified cmdlet `Get-AzMLWorkspaceOnlineEndpoint`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-ComputeType` from `EndpointComputeType` to `String`
   - Changed the type of parameter `-OrderBy` from `OrderString` to `String`
* Modified cmdlet `Get-AzMLWorkspaceOnlineEndpointToken`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Invoke-AzMLWorkspaceDiagnose`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Output type changed from ``IDiagnoseResponseResultValue`` to ``IDiagnoseResponseResult``
* Modified cmdlet `New-AzMLWorkspace`
   - Removed parameter `-IdentityType`
   - Added parameters `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-EncryptionStatus` from `EncryptionStatus` to `String`
   - Changed the type of parameter `-Kind` from `WorkspaceType` to `String`
   - Changed the type of parameter `-ManagedNetworkIsolationMode` from `IsolationMode` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccess` to `String`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
   - Changed the type of parameter `-Status` from `ManagedNetworkStatus` to `String`
* Modified cmdlet `New-AzMLWorkspaceAadAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceAccessKeyAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceAccountKeyAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceAksObject`
   - Changed the type of parameter `-ClusterPurpose` from `ClusterPurpose` to `String`
   - Changed the type of parameter `-LoadBalancerType` from `LoadBalancerType` to `String`
   - Changed the type of parameter `-SslStatus` from `SslConfigStatus` to `String`
* Modified cmdlet `New-AzMLWorkspaceAmlComputeObject`
   - Changed the type of parameter `-OSType` from `OSType` to `String`
   - Changed the type of parameter `-RemoteLoginPortPublicAccess` from `RemoteLoginPortPublicAccess` to `String`
   - Changed the type of parameter `-VMPriority` from `VMPriority` to `String`
* Modified cmdlet `New-AzMLWorkspaceApiKeyAuthWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceBatchDeployment`
   - Removed parameter `-IdentityType`
   - Added parameters `-BatchEndpointInputObject`, `-WorkspaceInputObject`, `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-DeploymentConfigurationType` from `BatchDeploymentConfigurationType` to `String`
   - Changed the type of parameter `-LoggingLevel` from `BatchLoggingLevel` to `String`
   - Changed the type of parameter `-OutputAction` from `BatchOutputAction` to `String`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
* Modified cmdlet `New-AzMLWorkspaceBatchEndpoint`
   - Removed parameter `-IdentityType`
   - Added parameters `-WorkspaceInputObject`, `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-AuthMode` from `EndpointAuthMode` to `String`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
* Modified cmdlet `New-AzMLWorkspaceCodeVersion`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceCommandJobObject`
   - Added parameters `-QueueSettingJobTier`, `-ResourceDockerArg`, `-ResourceShmSize`, `-ComponentId`, `-NotificationSettingEmail`, `-NotificationSettingEmailOn`, `-NotificationSettingWebhook`
   - Changed the type of parameter `-DistributionType` from `DistributionType` to `String`
   - Changed the type of parameter `-IdentityType` from `IdentityConfigurationType` to `String`
* Modified cmdlet `New-AzMLWorkspaceComponentContainer`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceComponentVersion`
   - Added parameters `-ComponentInputObject`, `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceCompute`
   - Removed parameter `-IdentityType`
   - Added parameters `-WorkspaceInputObject`, `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
* Modified cmdlet `New-AzMLWorkspaceComputeInstanceObject`
   - Removed parameters `-LastOperationName`, `-LastOperationStatus`, `-LastOperationTime`, `-LastOperationTrigger`, `-VersionRuntime`
   - Added parameters `-Location`, `-CustomService`
   - Changed the type of parameter `-ApplicationSharingPolicy` from `ApplicationSharingPolicy` to `String`
   - Changed the type of parameter `-AuthorizationType` from `ComputeInstanceAuthorizationType` to `String`
   - Changed the type of parameter `-SshSettingSshPublicAccess` from `SshPublicAccess` to `String`
* Modified cmdlet `New-AzMLWorkspaceComputeStartStopScheduleObject`
   - Changed the type of parameter `-Action` from `ComputePowerAction` to `String`
   - Changed the type of parameter `-RecurrenceFrequency` from `ComputeRecurrenceFrequency` to `String`
   - Changed the type of parameter `-ScheduleProvisioningStatus` from `ScheduleProvisioningState` to `String`
   - Changed the type of parameter `-ScheduleStatus` from `ScheduleStatus` to `String`
   - Changed the type of parameter `-ScheduleWeekDay` from `ComputeWeekDay[]` to `String[]`
   - Changed the type of parameter `-Status` from `ScheduleStatus` to `String`
   - Changed the type of parameter `-TriggerType` from `ComputeTriggerType` to `String`
   - Output type changed from ``ComputeStartStopSchedule`` to 
* Modified cmdlet `New-AzMLWorkspaceConnection`
   - Changed the type of parameter `-AuthType` from `ConnectionAuthType` to `String`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceCustomKeysWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceCustomModelJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
   - Changed the type of parameter `-Mode` from `InputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceCustomModelJobOutputObject`
   - Changed the type of parameter `-Type` from `JobOutputType` to `String`
   - Changed the type of parameter `-Mode` from `OutputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceDatabricksObject`
   - Added parameter `-Location`
* Modified cmdlet `New-AzMLWorkspaceDataContainer`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-DataType` from `DataType` to `String`
* Modified cmdlet `New-AzMLWorkspaceDataFactoryObject`
   - Added parameter `-Location`
* Modified cmdlet `New-AzMLWorkspaceDataLakeAnalyticsObject`
   - Added parameter `-Location`
* Modified cmdlet `New-AzMLWorkspaceDataPathAssetReferenceObject`
   - Removed parameter `-ReferenceType`
* Modified cmdlet `New-AzMLWorkspaceDatastore`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceDatastoreBlobObject`
   - Added parameters `-ResourceGroup`, `-SubscriptionId`
   - Changed the type of parameter `-ServiceDataAccessAuthIdentity` from `ServiceDataAccessAuthIdentity` to `String`
* Modified cmdlet `New-AzMLWorkspaceDatastoreDataLakeGen1Object`
   - Added parameters `-ResourceGroup`, `-SubscriptionId`
   - Changed the type of parameter `-ServiceDataAccessAuthIdentity` from `ServiceDataAccessAuthIdentity` to `String`
* Modified cmdlet `New-AzMLWorkspaceDatastoreDataLakeGen2Object`
   - Added parameters `-ResourceGroup`, `-SubscriptionId`
   - Changed the type of parameter `-ServiceDataAccessAuthIdentity` from `ServiceDataAccessAuthIdentity` to `String`
* Modified cmdlet `New-AzMLWorkspaceDatastoreFileObject`
   - Added parameters `-Credentials`, `-ResourceGroup`, `-SubscriptionId`
   - Changed the type of parameter `-ServiceDataAccessAuthIdentity` from `ServiceDataAccessAuthIdentity` to `String`
* Modified cmdlet `New-AzMLWorkspaceDataVersion`
   - Added parameters `-DataInputObject`, `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-DataType` from `DataType` to `String`
* Modified cmdlet `New-AzMLWorkspaceEnvironmentVersion`
   - Added parameters `-EnvironmentInputObject`, `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-AutoRebuild` from `AutoRebuildSetting` to `String`
   - Changed the type of parameter `-OSType` from `OperatingSystemType` to `String`
* Modified cmdlet `New-AzMLWorkspaceHDInsightObject`
   - Added parameter `-Location`
* Modified cmdlet `New-AzMLWorkspaceIdAssetReferenceObject`
   - Removed parameter `-ReferenceType`
* Modified cmdlet `New-AzMLWorkspaceJob`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceLiteralJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
* Modified cmdlet `New-AzMLWorkspaceManagedIdentityAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceMLFlowModelJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
   - Changed the type of parameter `-Mode` from `InputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceMLFlowModelJobOutputObject`
   - Changed the type of parameter `-Type` from `JobOutputType` to `String`
   - Changed the type of parameter `-Mode` from `OutputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceMLTableJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
   - Changed the type of parameter `-Mode` from `InputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceMLTableJobOutputObject`
   - Changed the type of parameter `-Type` from `JobOutputType` to `String`
   - Changed the type of parameter `-Mode` from `OutputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceModelContainer`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceModelVersion`
   - Added parameters `-ModelInputObject`, `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMLWorkspaceNoneAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceOAuth2AuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceOnlineDeployment`
   - Removed parameter `-IdentityType`
   - Added parameters `-OnlineEndpointInputObject`, `-WorkspaceInputObject`, `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-EndpointComputeType` from `EndpointComputeType` to `String`
   - Changed the type of parameter `-DataCollectorRollingRate` from `RollingRateType` to `String`
   - Changed the type of parameter `-EgressPublicNetworkAccess` from `EgressPublicNetworkAccessType` to `String`
   - Changed the type of parameter `-ScaleType` from `ScaleType` to `String`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
* Modified cmdlet `New-AzMLWorkspaceOnlineEndpoint`
   - Removed parameter `-IdentityType`
   - Added parameters `-WorkspaceInputObject`, `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-AuthMode` from `EndpointAuthMode` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccessType` to `String`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
* Modified cmdlet `New-AzMLWorkspaceOnlineEndpointKey`
   - Added parameters `-WorkspaceInputObject`, `-Body`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-KeyType` from `KeyType` to `String`
* Modified cmdlet `New-AzMLWorkspaceOutputPathAssetReferenceObject`
   - Removed parameter `-ReferenceType`
* Modified cmdlet `New-AzMLWorkspacePatAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspacePipelineJobObject`
   - Added parameters `-SourceJobId`, `-ComponentId`, `-NotificationSettingEmail`, `-NotificationSettingEmailOn`, `-NotificationSettingWebhook`
   - Changed the type of parameter `-IdentityType` from `IdentityConfigurationType` to `String`
* Modified cmdlet `New-AzMLWorkspaceQuotaPropertiesObject`
   - Changed the type of parameter `-Unit` from `QuotaUnit` to `String`
* Modified cmdlet `New-AzMLWorkspaceSasAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceServicePrincipalAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceSharedPrivateLinkResourceObject`
   - Changed the type of parameter `-Status` from `PrivateEndpointServiceConnectionStatus` to `String`
* Modified cmdlet `New-AzMLWorkspaceSweepJobObject`
   - Added parameters `-LimitJobLimitsType`, `-QueueSettingJobTier`, `-ResourceDockerArg`, `-ResourceShmSize`, `-ComponentId`, `-NotificationSettingEmail`, `-NotificationSettingEmailOn`, `-NotificationSettingWebhook`
   - Changed the type of parameter `-ObjectiveGoal` from `Goal` to `String`
   - Changed the type of parameter `-SamplingAlgorithmType` from `SamplingAlgorithmType` to `String`
   - Changed the type of parameter `-DistributionType` from `DistributionType` to `String`
   - Changed the type of parameter `-EarlyTerminationPolicyType` from `EarlyTerminationPolicyType` to `String`
   - Changed the type of parameter `-IdentityType` from `IdentityConfigurationType` to `String`
* Modified cmdlet `New-AzMLWorkspaceSynapseSparkObject`
   - Added parameter `-Location`
* Modified cmdlet `New-AzMLWorkspaceTritonModelJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
   - Changed the type of parameter `-Mode` from `InputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceTritonModelJobOutputObject`
   - Changed the type of parameter `-Type` from `JobOutputType` to `String`
   - Changed the type of parameter `-Mode` from `OutputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceUriFileJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
   - Changed the type of parameter `-Mode` from `InputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceUriFileJobOutputObject`
   - Changed the type of parameter `-Type` from `JobOutputType` to `String`
   - Changed the type of parameter `-Mode` from `OutputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceUriFolderJobInputObject`
   - Changed the type of parameter `-Type` from `JobInputType` to `String`
   - Changed the type of parameter `-Mode` from `InputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceUriFolderJobOutputObject`
   - Changed the type of parameter `-Type` from `JobOutputType` to `String`
   - Changed the type of parameter `-Mode` from `OutputDeliveryMode` to `String`
* Modified cmdlet `New-AzMLWorkspaceUsernamePasswordAuthTypeWorkspaceConnectionPropertiesObject`
   - Changed the type of parameter `-Category` from `ConnectionCategory` to `String`
   - Changed the type of parameter `-ValueFormat` from `ValueFormat` to `String`
* Modified cmdlet `New-AzMLWorkspaceVirtualMachineObject`
   - Added parameter `-Location`
* Modified cmdlet `Remove-AzMLWorkspaceBatchDeployment`
   - Added parameters `-BatchEndpointInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceBatchEndpoint`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceCodeVersion`
   - Added parameters `-CodeInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceComponentContainer`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceComponentVersion`
   - Added parameters `-ComponentInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceCompute`
   - Added parameter `-WorkspaceInputObject`
   - Changed the type of parameter `-UnderlyingResourceAction` from `UnderlyingResourceAction` to `String`
* Modified cmdlet `Remove-AzMLWorkspaceConnection`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceDataContainer`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceDatastore`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceDataVersion`
   - Added parameters `-DataInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceEnvironmentContainer`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceEnvironmentVersion`
   - Added parameters `-EnvironmentInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceJob`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceModelContainer`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceModelVersion`
   - Added parameters `-ModelInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceOnlineDeployment`
   - Added parameters `-OnlineEndpointInputObject`, `-WorkspaceInputObject`
* Modified cmdlet `Remove-AzMLWorkspaceOnlineEndpoint`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Restart-AzMLWorkspaceCompute`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Start-AzMLWorkspaceCompute`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Stop-AzMLWorkspaceCompute`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Stop-AzMLWorkspaceJob`
   - Added parameter `-WorkspaceInputObject`
* Modified cmdlet `Update-AzMLServiceQuota`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzMLWorkspace`
   - Removed parameter `-IdentityType`
   - Added parameters `-AllowPublicAccessWhenBehindVnet`, `-AssociatedWorkspace`, `-DiscoveryUrl`, `-EnableSystemAssignedIdentity`, `-EncryptionStatus`, `-EncryptionUserAssignedIdentity`, `-HbiWorkspace`, `-HubResourceId`, `-KeyVaultArmId`, `-KeyVaultId`, `-KeyVaultIdentityClientId`, `-Kind`, `-Location`, `-SharedPrivateLinkResource`, `-StorageAccountId`, `-WorkspaceHubConfigAdditionalWorkspaceStorageAccount`, `-WorkspaceHubConfigDefaultWorkspaceResourceGroup`
   - Changed the type of parameter `-ManagedNetworkIsolationMode` from `IsolationMode` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccess` to `String`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
   - Changed the type of parameter `-Status` from `ManagedNetworkStatus` to `String`
* Modified cmdlet `Update-AzMLWorkspaceBatchDeployment`
   - Added parameters `-BatchEndpointInputObject`, `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzMLWorkspaceBatchEndpoint`
   - Removed parameter `-IdentityType`
   - Added parameters `-WorkspaceInputObject`, `-AuthMode`, `-DefaultDeploymentName`, `-Description`, `-EnableSystemAssignedIdentity`, `-EndpointProperties`, `-Kind`, `-SkuCapacity`, `-SkuFamily`, `-SkuName`, `-SkuSize`, `-SkuTier`
* Modified cmdlet `Update-AzMLWorkspaceCompute`
   - Added parameters `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Update-AzMLWorkspaceOnlineDeployment`
   - Added parameters `-OnlineEndpointInputObject`, `-WorkspaceInputObject`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
* Modified cmdlet `Update-AzMLWorkspaceOnlineEndpoint`
   - Removed parameter `-IdentityType`
   - Added parameters `-WorkspaceInputObject`, `-AuthMode`, `-Compute`, `-Description`, `-EnableSystemAssignedIdentity`, `-EndpointPropertiesBaseProperty`, `-Kind`, `-MirrorTraffic`, `-PublicNetworkAccess`, `-SkuCapacity`, `-SkuFamily`, `-SkuName`, `-SkuSize`, `-SkuTier`, `-Traffic`
* Added cmdlet `Update-AzMLWorkspaceCodeVersion`, `Update-AzMLWorkspaceComponentContainer`, `Update-AzMLWorkspaceComponentVersion`, `Update-AzMLWorkspaceConnection`, `Update-AzMLWorkspaceDataContainer`, `Update-AzMLWorkspaceDataVersion`, `Update-AzMLWorkspaceEnvironmentVersion`, `Update-AzMLWorkspaceJob`, `Update-AzMLWorkspaceModelContainer`, `Update-AzMLWorkspaceModelVersion`
#### Az.ManagedServices 4.0.0 
* Modified cmdlet `New-AzManagedServicesAssignment`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzManagedServicesDefinition`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzManagedServicesEligibleAuthorizationObject`
   - Changed the type of parameter `-JustInTimeAccessPolicyMultiFactorAuthProvider` from `MultiFactorAuthProvider` to `String`
* Added cmdlet `Update-AzManagedServicesDefinition`
#### Az.Migrate 3.0.0 
* Modified cmdlet `Get-AzMigrateReplicationFabric`
   - Added parameter `-VaultInputObject`
* Modified cmdlet `Get-AzMigrateReplicationPolicy`
   - Added parameter `-VaultInputObject`
* Modified cmdlet `Get-AzMigrateReplicationProtectionContainer`
   - Added parameters `-ReplicationFabricInputObject`, `-VaultInputObject`
* Modified cmdlet `Get-AzMigrateReplicationProtectionContainerMapping`
   - Added parameters `-ReplicationFabricInputObject`, `-ReplicationProtectionContainerInputObject`, `-VaultInputObject`
* Modified cmdlet `Get-AzMigrateReplicationRecoveryServicesProvider`
   - Added parameters `-ReplicationFabricInputObject`, `-VaultInputObject`
* Modified cmdlet `Get-AzMigrateRunAsAccount`
   - Added parameters `-SubscriptionInputObject`, `-VMwareSiteInputObject`
* Modified cmdlet `Get-AzMigrateSite`
   - `SupportsShouldProcess` changed from False to True
   - Added parameters `-SubscriptionInputObject`, `-Subscription1InputObject`, `-Subscription2InputObject`
* Modified cmdlet `Get-AzMigrateSolution`
   - Added parameter `-MigrateProjectInputObject`
* Modified cmdlet `New-AzMigrateReplicationPolicy`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzMigrateReplicationProtectionContainerMapping`
   - Added parameters `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Register-AzMigrateProjectTool`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Output type changed from ``Boolean`` to ``IRegistrationResult``
#### Az.Monitor 8.0.0 
* Modified cmdlet `New-AzMonitorWorkspace`
   - Added parameters `-EnableSystemAssignedIdentity`, `-MetricEnableAccessUsingResourcePermission`, `-PublicNetworkAccess`, `-UserAssignedIdentity`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzPipelineGroup`
   - Removed parameter `-NetworkingConfiguration`
   - Added parameters `-DistributionMaxInstancesPerHost`, `-ExecutionPlacementConstraint`, `-TlsConfiguration`
* Modified cmdlet `Update-AzMonitorWorkspace`
   - Added parameters `-EnableSystemAssignedIdentity`, `-MetricEnableAccessUsingResourcePermission`, `-PublicNetworkAccess`, `-UserAssignedIdentity`
* Modified cmdlet `Update-AzPipelineGroup`
   - Removed parameter `-NetworkingConfiguration`
   - Added parameters `-DistributionMaxInstancesPerHost`, `-ExecutionPlacementConstraint`, `-TlsConfiguration`
* Added cmdlet `Add-AzMonitorWorkspaceIssueAlert`, `Add-AzMonitorWorkspaceIssueInvestigationResult`, `Add-AzMonitorWorkspaceIssueResource`, `Get-AzMonitorWorkspaceIssue`, `Get-AzMonitorWorkspaceIssueAlert`, `Get-AzMonitorWorkspaceIssueResource`, `Get-AzMonitorWorkspaceMetricsContainer`, `Invoke-AzMonitorWorkspaceFetchIssueBackgroundVisualization`, `Invoke-AzMonitorWorkspaceFetchIssueInvestigationResult`, `New-AzMonitorWorkspaceIssue`, `New-AzMonitorWorkspaceMetricsContainer`, `Remove-AzMonitorWorkspaceIssue`, `Set-AzMonitorWorkspace`, `Set-AzMonitorWorkspaceIssueBackgroundVisualization`, `Set-AzMonitorWorkspaceMetricsContainer`, `Update-AzMonitorWorkspaceIssue`, `Update-AzMonitorWorkspaceIssueAlert`, `Update-AzMonitorWorkspaceIssueResource`, `Update-AzMonitorWorkspaceMetricsContainer`
#### Az.MySql 2.0.0 
* Removed cmdlet `Get-AzMySqlConfiguration`, `Get-AzMySqlConnectionString`, `Get-AzMySqlFirewallRule`
* Modified cmdlet `Get-AzMySqlFlexibleServer`
   - Output type changed from ``IServerAutoGenerated`` to ``IServer``
* Modified cmdlet `Get-AzMySqlFlexibleServerConfiguration`
   - Added parameter `-FlexibleServerInputObject`
   - Output type changed from ``IConfigurationAutoGenerated`` to ``IConfiguration``
* Modified cmdlet `Get-AzMySqlFlexibleServerDatabase`
   - Added parameter `-FlexibleServerInputObject`
   - Output type changed from ``IDatabaseAutoGenerated`` to ``IDatabase``
* Modified cmdlet `Get-AzMySqlFlexibleServerFirewallRule`
   - Added parameter `-FlexibleServerInputObject`
   - Output type changed from ``IFirewallRuleAutoGenerated`` to ``IFirewallRule``
* Removed cmdlet `Get-AzMySqlReplica`, `Get-AzMySqlServer`, `Get-AzMySqlVirtualNetworkRule`, `New-AzMySqlFirewallRule`
* Modified cmdlet `New-AzMySqlFlexibleServer`
   - Changed the type of parameter `-StorageAutogrow` from `StorageAutogrow` to `String`
   - Changed the type of parameter `-Version` from `ServerVersion` to `String`
   - Output type changed from ``IServerAutoGenerated`` to ``IServer``
* Modified cmdlet `New-AzMySqlFlexibleServerDatabase`
   - Added parameters `-FlexibleServerInputObject`, `-JsonFilePath`, `-JsonString`
   - Output type changed from ``IDatabaseAutoGenerated`` to ``IDatabase``
* Modified cmdlet `New-AzMySqlFlexibleServerReplica`
   - Changed the type of parameter `-Master` from `IServerAutoGenerated` to `IServer`
   - Output type changed from ``IServerAutoGenerated`` to ``IServer``
* Removed cmdlet `New-AzMySqlReplica`, `New-AzMySqlServer`, `New-AzMySqlVirtualNetworkRule`, `Remove-AzMySqlFirewallRule`
* Modified cmdlet `Remove-AzMySqlFlexibleServerDatabase`
   - Added parameter `-FlexibleServerInputObject`
* Modified cmdlet `Remove-AzMySqlFlexibleServerFirewallRule`
   - Added parameter `-FlexibleServerInputObject`
* Removed cmdlet `Remove-AzMySqlServer`, `Remove-AzMySqlVirtualNetworkRule`
* Modified cmdlet `Restart-AzMySqlFlexibleServer`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-RestartWithFailover` from `EnableStatusEnum` to `String`
* Removed cmdlet `Restart-AzMySqlServer`
* Modified cmdlet `Restore-AzMySqlFlexibleServer`
   - Changed the type of parameter `-InputObject` from `IServerAutoGenerated` to `IServer`
   - Output type changed from ``IServerAutoGenerated`` to ``IServer``
* Removed cmdlet `Restore-AzMySqlServer`, `Update-AzMySqlConfiguration`, `Update-AzMySqlFirewallRule`
* Modified cmdlet `Update-AzMySqlFlexibleServer`
   - Changed the type of parameter `-SkuTier` from `SkuTier` to `String`
   - Changed the type of parameter `-StorageAutogrow` from `StorageAutogrow` to `String`
   - Output type changed from ``IServerAutoGenerated`` to ``IServer``
* Modified cmdlet `Update-AzMySqlFlexibleServerAdvancedThreatProtectionSetting`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-State` from `AdvancedThreatProtectionState` to `String`
* Modified cmdlet `Update-AzMySqlFlexibleServerConfiguration`
   - Output type changed from ``IConfigurationAutoGenerated`` to ``IConfiguration``
* Modified cmdlet `Update-AzMySqlFlexibleServerFirewallRule`
   - Output type changed from ``IFirewallRuleAutoGenerated`` to ``IFirewallRule``
* Removed cmdlet `Update-AzMySqlServer`, `Update-AzMySqlServerConfigurationsList`, `Update-AzMySqlVirtualNetworkRule`
#### Az.Network 8.0.0 
* Modified cmdlet `New-AzFirewallPolicy`
   - Changed the type of parameter `-UserAssignedIdentityId` from `String` to `String[]`
* Modified cmdlet `Set-AzFirewallPolicy`
   - Changed the type of parameter `-UserAssignedIdentityId` from `String` to `String[]`
#### Az.NetworkCloud 2.0.1 
* Modified cmdlet `Get-AzNetworkCloudAgentPool`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudBareMetalMachine`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudBareMetalMachineKeySet`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudBmcKeySet`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudCluster`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudClusterManager`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudConsole`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudKubernetesCluster`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudKubernetesClusterFeature`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudL2Network`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudL3Network`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudMetricsConfiguration`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudRack`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudServicesNetwork`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudStorageAppliance`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudTrunkedNetwork`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudVirtualMachine`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Get-AzNetworkCloudVolume`
   - Added parameters `-SkipToken`, `-Top`
* Modified cmdlet `Invoke-AzNetworkCloudBareMetalMachineReplace`
   - Added parameters `-SafeguardMode`, `-StoragePolicy`
* Modified cmdlet `New-AzNetworkCloudBareMetalMachineKeySet`
   - Added parameter `-PrivilegeLevelName`
* Modified cmdlet `New-AzNetworkCloudCluster`
   - Added parameter `-CommandOutputSettingOverride`
* Modified cmdlet `New-AzNetworkCloudServicesNetwork`
   - Added parameters `-StorageOptionMode`, `-StorageOptionSizeMiB`, `-StorageOptionStorageApplianceId`
* Modified cmdlet `New-AzNetworkCloudVirtualMachine`
   - Added parameters `-EnableSystemAssignedIdentity`, `-NetworkDataContent`, `-UserAssignedIdentity`, `-UserDataContent`
* Modified cmdlet `New-AzNetworkCloudVolume`
   - Added parameter `-StorageApplianceId`
* Modified cmdlet `Update-AzNetworkCloudCluster`
   - Added parameter `-CommandOutputSettingOverride`
* Modified cmdlet `Update-AzNetworkCloudServicesNetwork`
   - Added parameters `-StorageOptionMode`, `-StorageOptionSizeMiB`, `-StorageOptionStorageApplianceId`
* Modified cmdlet `Update-AzNetworkCloudVirtualMachine`
   - Removed parameters `-JsonFilePath`, `-JsonString`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
* Added cmdlet `Invoke-AzNetworkCloudBareMetalMachineRestrictedDataExtract`, `Invoke-AzNetworkCloudStorageApplianceReadCommand`
#### Az.PolicyInsights 2.0.0 
* Modified cmdlet `Get-AzPolicyAttestation`
   - Added parameters `-SubscriptionId`, `-InputObject`
   - Added parameter alias `AttestationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Scope` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Removed parameter alias `Id` from parameter `-ResourceId`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Filter` ValidateNotNullOrEmpty changed from `True` to `False`
   - Output type changed from ``PSAttestation`` to ``IAttestation``
* Modified cmdlet `Get-AzPolicyEvent`
   - `SupportsShouldProcess` changed from False to True
   - Added parameter `-Expand`
   - Parameter `-ManagementGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-SubscriptionId` from `String` to `String[]`
   - Parameter `-SubscriptionId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicySetDefinitionName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyDefinitionName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyAssignmentName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-OrderBy` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Select` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Filter` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Apply` ValidateNotNullOrEmpty changed from `True` to `False`
   - Output type changed from ``PolicyEvent`` to ``IPolicyEvent``
* Modified cmdlet `Get-AzPolicyMetadata`
   - Added parameter alias `ResourceName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Output type changed from ``PSPolicyMetadata`` to ``IPolicyMetadata``
* Modified cmdlet `Get-AzPolicyRemediation`
   - Removed parameter `-ManagementGroupName`
   - Added parameters `-SubscriptionId`, `-ManagementGroupId`, `-InputObject`
   - Added parameter alias `RemediationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Scope` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Removed parameter alias `Id` from parameter `-ResourceId`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Filter` ValidateNotNullOrEmpty changed from `True` to `False`
   - Output type changed from ``PSRemediation`` to ``IRemediation``
* Modified cmdlet `Get-AzPolicyState`
   - `SupportsShouldProcess` changed from False to True
   - Parameter `-ManagementGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-SubscriptionId` from `String` to `String[]`
   - Parameter `-SubscriptionId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicySetDefinitionName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyDefinitionName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyAssignmentName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-OrderBy` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Select` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Filter` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Apply` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Expand` ValidateNotNullOrEmpty changed from `True` to `False`
   - Output type changed from ``PolicyState`` to ``IPolicyState``
* Modified cmdlet `Get-AzPolicyStateSummary`
   - `SupportsShouldProcess` changed from False to True
   - Parameter `-ManagementGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-SubscriptionId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicySetDefinitionName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyDefinitionName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyAssignmentName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Filter` ValidateNotNullOrEmpty changed from `True` to `False`
   - Output type changed from ``PolicyStateSummary`` to ``ISummary``
* Modified cmdlet `New-AzPolicyAttestation`
   - Added parameters `-SubscriptionId`, `-InputObject`
   - Added parameter alias `AttestationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyAssignmentId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-ExpiresOn` from `Nullable`1[System.DateTime]` to `DateTime`
   - Changed the type of parameter `-Evidence` from `PSAttestationEvidence[]` to `IAttestationEvidence[]`
   - Changed the type of parameter `-AssessmentDate` from `Nullable`1[System.DateTime]` to `DateTime`
   - Output type changed from ``PSAttestation`` to ``IAttestation``
* Modified cmdlet `Remove-AzPolicyAttestation`
   - Added parameter `-SubscriptionId`
   - Added parameter alias `AttestationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Scope` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-InputObject` from `PSAttestation` to `IPolicyInsightsIdentity`
* Modified cmdlet `Remove-AzPolicyRemediation`
   - Removed parameter `-ManagementGroupName`
   - Added parameters `-SubscriptionId`, `-ManagementGroupId`
   - Added parameter alias `RemediationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Scope` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Removed parameter alias `Id` from parameter `-ResourceId`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-InputObject` from `PSRemediation` to `IPolicyInsightsIdentity`
* Removed cmdlet `Set-AzPolicyAttestation`
* Modified cmdlet `Start-AzPolicyComplianceScan`
   - Added parameters `-SubscriptionId`, `-NoWait`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
* Modified cmdlet `Start-AzPolicyRemediation`
   - Removed parameters `-ManagementGroupName`, `-LocationFilter`, `-ParallelDeploymentCount`, `-FailureThreshold`
   - Added parameters `-SubscriptionId`, `-ManagementGroupId`, `-InputObject`, `-FailureThresholdPercentage`, `-FilterLocation`, `-FilterResourceId`, `-ParallelDeployment`, `-NoWait`
   - Added parameter alias `RemediationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Scope` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyAssignmentId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-PolicyDefinitionReferenceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-ResourceCount` from `Nullable`1[System.Int32]` to `Int32`
   - Output type changed from ``PSRemediation`` to ``IRemediation``
* Modified cmdlet `Stop-AzPolicyRemediation`
   - Removed parameters `-ManagementGroupName`, `-PassThru`
   - Added parameters `-SubscriptionId`, `-ManagementGroupId`, `-NoWait`
   - Added parameter alias `RemediationName` to parameter `-Name`
   - Parameter `-Name` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-Scope` ValidateNotNullOrEmpty changed from `True` to `False`
   - Parameter `-ResourceGroupName` ValidateNotNullOrEmpty changed from `True` to `False`
   - Removed parameter alias `Id` from parameter `-ResourceId`
   - Parameter `-ResourceId` ValidateNotNullOrEmpty changed from `True` to `False`
   - Changed the type of parameter `-InputObject` from `PSRemediation` to `IPolicyInsightsIdentity`
   - Output type changed from ``Boolean`` to ``IRemediation``
* Added cmdlet `Update-AzPolicyAttestation`
#### Az.RedisEnterpriseCache 2.0.0 
* Modified cmdlet `Get-AzRedisEnterpriseCacheAccessPolicyAssignment`
   - Added parameters `-DatabaseInputObject`, `-RedisEnterpriseInputObject`
* Modified cmdlet `Get-AzRedisEnterpriseCacheOperationStatus`
   - Added parameter `-LocationInputObject`
* Modified cmdlet `Invoke-AzRedisEnterpriseCacheDatabaseFlush`
   - Removed parameter `-Parameter`
   - Added parameters `-RedisEnterpriseInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Invoke-AzRedisEnterpriseCacheForceDatabaseLinkToReplicationGroup`
   - Removed parameter `-Parameter`
   - Added parameters `-RedisEnterpriseInputObject`, `-JsonFilePath`, `-JsonString`, `-PassThru`
* Modified cmdlet `Invoke-AzRedisEnterpriseCacheForceDatabaseUnlink`
   - Removed parameter `-Parameter`
   - Added parameters `-RedisEnterpriseInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzRedisEnterpriseCache`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`
   - Changed the type of parameter `-Sku` from `SkuName` to `String`
   - Changed the type of parameter `-MinimumTlsVersion` from `TlsVersion` to `String`
   - Changed the type of parameter `-ClientProtocol` from `Protocol` to `String`
   - Changed the type of parameter `-EvictionPolicy` from `EvictionPolicy` to `String`
   - Changed the type of parameter `-ClusteringPolicy` from `ClusteringPolicy` to `String`
   - Changed the type of parameter `-AccessKeysAuthentication` from `AccessKeysAuthentication` to `String`
   - Changed the type of parameter `-HighAvailability` from `HighAvailability` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccess` to `String`
   - Changed the type of parameter `-KeyEncryptionKeyIdentityType` from `CmkIdentityType` to `String`
   - Changed the type of parameter `-AofPersistenceFrequency` from `AofFrequency` to `String`
   - Changed the type of parameter `-RdbPersistenceFrequency` from `RdbFrequency` to `String`
* Modified cmdlet `New-AzRedisEnterpriseCacheAccessPolicyAssignment`
   - Removed parameters `-AccessPolicyAssignmentName`, `-Parameter`
   - Added parameters `-Name`, `-DatabaseInputObject`, `-RedisEnterpriseInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `New-AzRedisEnterpriseCacheDatabase`
   - Changed the type of parameter `-ClientProtocol` from `Protocol` to `String`
   - Changed the type of parameter `-EvictionPolicy` from `EvictionPolicy` to `String`
   - Changed the type of parameter `-ClusteringPolicy` from `ClusteringPolicy` to `String`
   - Changed the type of parameter `-AofPersistenceFrequency` from `AofFrequency` to `String`
   - Changed the type of parameter `-RdbPersistenceFrequency` from `RdbFrequency` to `String`
   - Changed the type of parameter `-AccessKeysAuthentication` from `AccessKeysAuthentication` to `String`
* Modified cmdlet `New-AzRedisEnterpriseCacheKey`
   - Changed the type of parameter `-KeyType` from `AccessKeyType` to `String`
* Modified cmdlet `Remove-AzRedisEnterpriseCacheAccessPolicyAssignment`
   - Added parameters `-DatabaseInputObject`, `-RedisEnterpriseInputObject`
* Modified cmdlet `Update-AzRedisEnterpriseCache`
   - Removed parameters `-IdentityType`, `-IdentityUserAssignedIdentity`
   - Added parameters `-EnableSystemAssignedIdentity`, `-UserAssignedIdentity`, `-Zone`
   - Changed the type of parameter `-HighAvailability` from `HighAvailability` to `String`
   - Changed the type of parameter `-KeyEncryptionKeyIdentityType` from `CmkIdentityType` to `String`
   - Changed the type of parameter `-MinimumTlsVersion` from `TlsVersion` to `String`
   - Changed the type of parameter `-PublicNetworkAccess` from `PublicNetworkAccess` to `String`
   - Changed the type of parameter `-Sku` from `SkuName` to `String`
* Modified cmdlet `Update-AzRedisEnterpriseCacheDatabase`
   - Changed the type of parameter `-AccessKeysAuthentication` from `AccessKeysAuthentication` to `String`
   - Changed the type of parameter `-ClientProtocol` from `Protocol` to `String`
   - Changed the type of parameter `-EvictionPolicy` from `EvictionPolicy` to `String`
   - Changed the type of parameter `-AofPersistenceFrequency` from `AofFrequency` to `String`
   - Changed the type of parameter `-RdbPersistenceFrequency` from `RdbFrequency` to `String`
* Modified cmdlet `Update-AzRedisEnterpriseCacheDatabaseDbRedisVersion`
   - Added parameters `-RedisEnterpriseInputObject`, `-PassThru`
#### Az.ResourceMover 2.0.0 
* Modified cmdlet `Add-AzResourceMoverMoveResource`
   - Added parameters `-MoveCollectionInputObject`, `-JsonFilePath`, `-JsonString`
* Modified cmdlet `Get-AzResourceMoverMoveResource`
   - Added parameter `-MoveCollectionInputObject`
* Modified cmdlet `Get-AzResourceMoverUnresolvedDependency`
   - Changed the type of parameter `-DependencyLevel` from `DependencyLevel` to `String`
* Modified cmdlet `Invoke-AzResourceMoverBulkRemove`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-MoveResourceInputType` from `MoveResourceInputType` to `String`
* Modified cmdlet `Invoke-AzResourceMoverCommit`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-MoveResourceInputType` from `MoveResourceInputType` to `String`
* Modified cmdlet `Invoke-AzResourceMoverDiscard`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-MoveResourceInputType` from `MoveResourceInputType` to `String`
* Modified cmdlet `Invoke-AzResourceMoverInitiateMove`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-MoveResourceInputType` from `MoveResourceInputType` to `String`
* Modified cmdlet `Invoke-AzResourceMoverPrepare`
   - Added parameters `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-MoveResourceInputType` from `MoveResourceInputType` to `String`
* Modified cmdlet `New-AzResourceMoverMoveCollection`
   - Removed parameter `-IdentityType`
   - Added parameters `-EnableSystemAssignedIdentity`, `-JsonFilePath`, `-JsonString`
   - Changed the type of parameter `-MoveType` from `MoveType` to `String`
* Modified cmdlet `Remove-AzResourceMoverMoveResource`
   - Added parameter `-MoveCollectionInputObject`
#### Az.Resources 10.0.0 
* Modified cmdlet `New-AzManagementGroupDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `New-AzResourceGroupDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `New-AzSubscriptionDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Remove-AzManagementGroupDeploymentStack`
   - Removed parameter `-InputObjet`
   - Added parameters `-InputObject`, `-ResourcesWithoutDeleteSupport`
* Modified cmdlet `Remove-AzResourceGroupDeploymentStack`
   - Removed parameter `-InputObjet`
   - Added parameters `-InputObject`, `-ResourcesWithoutDeleteSupport`
* Modified cmdlet `Remove-AzSubscriptionDeploymentStack`
   - Removed parameter `-InputObjet`
   - Added parameters `-InputObject`, `-ResourcesWithoutDeleteSupport`
* Modified cmdlet `Save-AzManagementGroupDeploymentStackTemplate`
   - Removed parameter `-InputObjet`
   - Added parameter `-InputObject`
* Modified cmdlet `Save-AzResourceGroupDeploymentStackTemplate`
   - Removed parameter `-InputObjet`
   - Added parameter `-InputObject`
* Modified cmdlet `Save-AzSubscriptionDeploymentStackTemplate`
   - Removed parameter `-InputObjet`
   - Added parameter `-InputObject`
* Modified cmdlet `Set-AzManagementGroupDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Set-AzResourceGroupDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Set-AzSubscriptionDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Test-AzManagementGroupDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Test-AzResourceGroupDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Test-AzSubscriptionDeploymentStack`
   - Added parameters `-ResourcesWithoutDeleteSupport`, `-ValidationLevel`
* Modified cmdlet `Get-AzPolicyAssignment`
   - Removed parameter `-BackwardCompatible`
   - Added parameter `-Expand`
* Modified cmdlet `Get-AzPolicyDefinition`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `Get-AzPolicyExemption`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `Get-AzPolicySetDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameter `-Expand`
* Modified cmdlet `New-AzPolicyAssignment`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `New-AzPolicyDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameters `-Version`, `-ExternalEvaluationEnforcementSettingMissingTokenAction`, `-ExternalEvaluationEnforcementSettingResultLifespan`, `-ExternalEvaluationEnforcementSettingRoleDefinitionId`, `-EndpointSettingKind`, `-EndpointSettingDetail`
* Modified cmdlet `New-AzPolicyExemption`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `New-AzPolicySetDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameter `-Version`
* Modified cmdlet `Remove-AzPolicyAssignment`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `Remove-AzPolicyDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameter `-Version`
* Modified cmdlet `Remove-AzPolicyExemption`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `Remove-AzPolicySetDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameter `-Version`
* Modified cmdlet `Update-AzPolicyAssignment`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `Update-AzPolicyDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameters `-Version`, `-ExternalEvaluationEnforcementSettingMissingTokenAction`, `-ExternalEvaluationEnforcementSettingResultLifespan`, `-ExternalEvaluationEnforcementSettingRoleDefinitionId`, `-EndpointSettingKind`, `-EndpointSettingDetail`
* Modified cmdlet `Update-AzPolicyExemption`
   - Removed parameter `-BackwardCompatible`
* Modified cmdlet `Update-AzPolicySetDefinition`
   - Removed parameter `-BackwardCompatible`
   - Added parameter `-Version`
#### Az.ServiceFabric 5.2.0 
* Added cmdlet `Enable-AzServiceFabricManagedClusterMaintenanceWindow`, `Get-AzServiceFabricManagedClusterMaintenanceWindowStatus`
#### Az.Sql 7.0.0 
* Modified cmdlet `New-AzSqlSyncGroup`
   - Removed parameters `-HubDatabaseAuthenticationType`, `-ResourceId`
* Modified cmdlet `New-AzSqlSyncMember`
   - Removed parameters `-MemberDatabaseAuthenticationType`, `-ResourceId`
* Modified cmdlet `Update-AzSqlSyncGroup`
   - Removed parameters `-HubDatabaseAuthenticationType`, `-ResourceId`, `-RemoveIdentityResourceId`
* Modified cmdlet `Update-AzSqlSyncMember`
   - Removed parameters `-MemberDatabaseAuthenticationType`, `-ResourceId`, `-RemoveIdentityResourceId`
#### Az.Storage 9.7.0 
* Modified cmdlet `Set-AzStorageObjectReplicationPolicy`
   - Added parameter `-EnableTagsReplication`
* Modified cmdlet `Update-AzStorageBlobServiceProperty`
   - Added parameters `-EnableStaticWebsite`, `-StaticWebsiteIndexDocument`, `-StaticWebsiteDefaultIndexDocumentPath`, `-StaticWebsiteErrorDocument404Path`
#### Az.StorageMover 2.1.0 
* Modified cmdlet `New-AzStorageMoverJobDefinition`
   - Added parameters `-Connection`, `-DataIntegrityValidation`, `-ExecutionTimeHour`, `-ExecutionTimeMinute`, `-PreservePermission`, `-ScheduleCronExpression`, `-ScheduleDaysOfMonth`, `-ScheduleDaysOfWeek`, `-ScheduleEndDate`, `-ScheduleFrequency`, `-ScheduleIsActive`, `-ScheduleStartDate`
* Modified cmdlet `Update-AzStorageMoverJobDefinition`
   - Added parameters `-Connection`, `-DataIntegrityValidation`, `-ExecutionTimeHour`, `-ExecutionTimeMinute`, `-ScheduleCronExpression`, `-ScheduleDaysOfMonth`, `-ScheduleDaysOfWeek`, `-ScheduleEndDate`, `-ScheduleFrequency`, `-ScheduleIsActive`, `-ScheduleStartDate`
* Added cmdlet `Get-AzStorageMoverConnection`, `New-AzStorageMoverConnection`, `New-AzStorageMoverS3WithHmacEndpoint`, `Remove-AzStorageMoverConnection`, `Update-AzStorageMoverS3WithHmacEndpoint`


